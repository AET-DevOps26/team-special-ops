# The playbook runs entirely on the control node (localhost): every task talks to
# the AKS API server through the kubeconfig that `az aks get-credentials` wrote,
# not over SSH. So the only "host" is the machine running ansible itself.
[local]
localhost ansible_connection=local ansible_python_interpreter="{{ ansible_playbook_python }}"
