[defaults]
inventory = inventory.ini
# The VM is freshly created each apply, so its SSH host key is always new —
# disable host-key checking so the first connection doesn't prompt/fail.
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
